buildscript {
    repositories {
        maven { url "http://platform-acceleration-lab-maven.s3.amazonaws.com" }
        jcenter()
    }

    dependencies {
        classpath "io.pivotal.pal.assignments:assignments-plugin:4.5.2"
    }
}

apply plugin: "io.pivotal.pal.assignments.plugin"

assignments {
    apiUrl = "https://cognizant-waveland.education.pivotal.io/"
    emails = [
            "sandeep.borhade@cognizant.com"
    ]
    cohortIdentifier = "pal-training-program-2020--architect-batch-1"
}

defaultTasks "tasks"